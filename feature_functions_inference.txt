=== Fichier source : E:\Workspace\Memory\Spc\notebooks\api\pipeline_v2.py ===

--- def prepare_features_v2 : NON TROUVÉE dans ce fichier ---

--- def _build_corrector_input (lignes 614 à 650) ---
  614 | 'def _build_corrector_input(rf_proba, X_small, struct_feats: dict,'
  615 | '                            n_expected: int):'
  616 | '    """'
  617 | "    Construit le vecteur d'entrée du correcteur en s'adaptant"
  618 | '    automatiquement aux dimensions attendues.'
  619 | ''
  620 | '    Dimensions possibles :'
  621 | '      508 = 3 (RF) + 500 (TF-IDF) + 5 (struct légères v1)'
  622 | '      523 = 3 (RF) + 500 (TF-IDF) + 20 (struct complètes v2)'
  623 | '    """'
  624 | '    struct_values = list(struct_feats.values())   # 20 valeurs'
  625 | ''
  626 | '    if n_expected == 523:'
  627 | '        # v2 : struct complètes (20)'
  628 | '        X_struct = csr_matrix([struct_values])'
  629 | '        return hstack([csr_matrix([rf_proba]), X_small, X_struct])'
  630 | ''
  631 | '    elif n_expected == 508:'
  632 | '        # v1 : 5 struct légères — utiliser les 5 premières (url_count...has_at)'
  633 | '        X_struct_v1 = csr_matrix([struct_values[:5]])'
  634 | '        return hstack([csr_matrix([rf_proba]), X_small, X_struct_v1])'
  635 | ''
  636 | '    elif n_expected == 503:'
  637 | '        # v1 ancienne : sans struct du tout'
  638 | '        return hstack([csr_matrix([rf_proba]), X_small])'
  639 | ''
  640 | '    else:'
  641 | '        # Fallback : construire à la bonne taille par padding/troncature'
  642 | '        base = hstack([csr_matrix([rf_proba]), X_small])   # 503'
  643 | '        current = base.shape[1]'
  644 | '        if current < n_expected:'
  645 | '            pad = csr_matrix(np.zeros((1, n_expected - current)))'
  646 | '            return hstack([base, pad])'
  647 | '        else:'
  648 | '            return base[:, :n_expected]'
  649 | ''
  650 | ''

--- def _apply_corrector (lignes 651 à 718) ---
  651 | 'def _apply_corrector(corrector, rf_proba_dict: dict,'
  652 | '                     X_tfidf, struct_feats: dict,'
  653 | '                     class_names: list) -> dict:'
  654 | '    """'
  655 | '    Fusion RF + correcteur SGD avec adaptation automatique des dimensions.'
  656 | ''
  657 | '    Résout l\'erreur : "X has 523 features, but SGDClassifier is expecting 508"'
  658 | '    en détectant les dimensions du correcteur existant et en adaptant'
  659 | "    le vecteur d'entrée en conséquence."
  660 | ''
  661 | '    Lors de la prochaine mise à jour via incremental_layer_v2.py,'
  662 | "    le correcteur sera réentraîné avec 523 features et l'adaptation"
  663 | '    ne sera plus nécessaire.'
  664 | '    """'
  665 | '    rf_proba = np.array([rf_proba_dict.get(c, 0.0) for c in class_names])'
  666 | ''
  667 | '    try:'
  668 | '        # TF-IDF réduit top-500'
  669 | '        if X_tfidf.shape[1] > 500:'
  670 | '            variances = np.asarray(X_tfidf.power(2).mean(axis=0)).flatten()'
  671 | '            top500    = np.argsort(variances)[-500:]'
  672 | '            X_small   = X_tfidf[:, top500]'
  673 | '        else:'
  674 | '            X_small = X_tfidf'
  675 | ''
  676 | '        # Détecter les dimensions du correcteur existant'
  677 | '        n_expected = _get_corrector_n_features(corrector)'
  678 | ''
  679 | "        # Construire le vecteur d'entrée adapté"
  680 | '        X_corr = _build_corrector_input(rf_proba, X_small, struct_feats, n_expected)'
  681 | ''
  682 | "        if not hasattr(corrector, 'predict_proba'):"
  683 | '            return rf_proba_dict'
  684 | ''
  685 | '        corr_proba = corrector.predict_proba(X_corr)[0]'
  686 | '        corr_conf  = float(corr_proba.max())'
  687 | '        corr_pred  = class_names[int(np.argmax(corr_proba))]'
  688 | ''
  689 | '        # Poids de base'
  690 | '        w_corr = 0.4 * corr_conf'
  691 | ''
  692 | '        # Boost spam : si le correcteur détecte du spam avec certitude,'
  693 | '        # augmenter son poids car RF sous-prédit systématiquement le spam'
  694 | "        if corr_pred == 'spam' and corr_conf > 0.50:"
  695 | '            w_corr = min(w_corr + 0.15, 0.55)'
  696 | '            logger.debug(f"Spam boost activé (w_corr={w_corr:.2f})")'
  697 | ''
  698 | '        # Garde de protection : si le RF est déjà très confiant sur phishing,'
  699 | "        # le correcteur ne doit pas pouvoir écraser ce signal — tant qu'il n'a"
  700 | '        # pas reçu un volume suffisant de vraies corrections phishing bien'
  701 | '        # calibrées, son jugement sur cette classe reste structurellement'
  702 | '        # moins fiable que celui du RF seul.'
  703 | "        rf_phish_idx = class_names.index('phishing') if 'phishing' in class_names else None"
  704 | '        if rf_phish_idx is not None and rf_proba[rf_phish_idx] > 0.70:'
  705 | '            w_corr = min(w_corr, 0.15)'
  706 | '            logger.debug(f"Protection RF phishing activée (w_corr plafonné à {w_corr:.2f})")'
  707 | ''
  708 | '        w_rf  = 1.0 - w_corr'
  709 | '        final = w_rf * rf_proba + w_corr * corr_proba'
  710 | '        final = final / final.sum()'
  711 | ''
  712 | '        return {c: float(p) for c, p in zip(class_names, final)}'
  713 | ''
  714 | '    except Exception as e:'
  715 | '        logger.warning(f"Correcteur erreur : {e} — RF seul")'
  716 | '        return rf_proba_dict'
  717 | ''
  718 | ''

--- def _predict_ml (lignes 810 à 823) ---
  810 | '    def _predict_ml(self, text: str):'
  811 | '        """RF prediction — retourne (proba_dict, X_tfidf, struct_feats)."""'
  812 | '        cleaned  = clean_text(text)'
  813 | '        X_tfidf  = self._tfidf.transform([cleaned])'
  814 | '        struct   = extract_structural_features(text)'
  815 | '        X_struct = csr_matrix([list(struct.values())])'
  816 | '        try:'
  817 | '            X     = hstack([X_tfidf, X_struct])'
  818 | '            proba = self._ml_model.predict_proba(X)[0]'
  819 | '        except Exception:'
  820 | '            proba = self._ml_model.predict_proba(X_tfidf)[0]'
  821 | '        proba_dict = {c: float(p) for c, p in zip(self._class_names, proba)}'
  822 | '        return proba_dict, X_tfidf, struct'
  823 | ''

--- def load_models (lignes 791 à 805) ---
  791 | '    def load_models(self):'
  792 | '        import warnings'
  793 | '        warnings.filterwarnings("ignore", category=UserWarning)'
  794 | ''
  795 | "        self._tfidf         = joblib.load(DATA_DIR / 'tfidf_vectorizer.pkl')"
  796 | "        self._ml_model      = joblib.load(DATA_DIR / 'best_model.pkl')"
  797 | "        self._label_encoder = joblib.load(DATA_DIR / 'label_encoder.pkl')"
  798 | '        self._class_names   = list(self._label_encoder.classes_)'
  799 | '        self._corrector     = _load_corrector()'
  800 | ''
  801 | '        logger.info(f"Modèle base  : {type(self._ml_model).__name__}")'
  802 | '        logger.info(f"Classes      : {self._class_names}")'
  803 | '        logger.info(f"Correcteur   : {\'actif\' if self._corrector else \'inactif\'}")'
  804 | '        logger.info(f"Whitelist    : {len(self._whitelist.domains)} domaines")'
  805 | ''

--- def load_corrector : NON TROUVÉE dans ce fichier ---

--- _load_corrector (lignes 594 à 604) ---
  594 | 'def _load_corrector():'
  595 | '    if CORRECTOR_PATH.exists():'
  596 | '        try:'
  597 | '            c = joblib.load(CORRECTOR_PATH)'
  598 | '            logger.info("Correcteur SGD chargé.")'
  599 | '            return c'
  600 | '        except Exception as e:'
  601 | '            logger.warning(f"Correcteur non chargé : {e}")'
  602 | '    return None'
  603 | ''
  604 | ''

